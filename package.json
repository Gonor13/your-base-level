{
  "name": "your-base-level",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "echo 'Static site: open public/index.html in browser'",
    "build": "echo 'No build step for static site'",
    "start": "echo 'Static site deployed on Vercel'"
  },
  "engines": {
    "node": ">=18"
  }
}
